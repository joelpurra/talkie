{
  "name": "@talkie/split-environment",
  "version": "0.0.0",
  "author": {
    "email": "code+github@joelpurra.com",
    "name": "Joel Purra",
    "url": "https://joelpurra.com/"
  },
  "dependencies": {
    "@talkie/split-environment-interfaces": "*",
    "@talkie/split-environment-node": "*",
    "@talkie/split-environment-webextension": "*"
  },
  "exports": {
    "./*": {
      "types": "./dist/esm/*",
      "import": "./dist/esm/*"
    }
  },
  "homepage": "https://joelpurra.com/projects/talkie/",
  "license": "GPL-3.0",
  "private": true,
  "scripts": {
    "build": "npm run --silent build:typescript && npm run --silent build:split-environment",
    "build:split-environment": "npm run --silent build:split-environment:node && npm run --silent build:split-environment:webextension",
    "build:split-environment:node": "echo 'Applying SPLIT_ENVIRONMENT hack for node' && echo '--- DISABLED ---' find ./dist/cjs -type f -print0 | echo '--- DISABLED ---' xargs -0 sed --in-place='~' 's/SPLIT_ENVIRONMENT/node/g'",
    "build:split-environment:webextension": "echo 'Applying SPLIT_ENVIRONMENT hack for webextension' && find ./dist/esm -type f -print0 | xargs -0 sed --in-place='~' 's/SPLIT_ENVIRONMENT/webextension/g'",
    "build:typescript": "tsc --build tsconfig.json",
    "clean": "rimraf ./dist",
    "rebuild": "npm run --silent clean && npm run --silent build",
    "watch": "npm run --silent build -- --watch"
  },
  "type": "module",
  "typesVersions": {
    "^4": {
      "*": [
        "src/*"
      ]
    }
  }
}
